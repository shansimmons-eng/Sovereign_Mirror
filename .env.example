# Copy to .env and fill in your own values. See ENVIRONMENT.md for what each
# key is used for, which are required, and where to get a free-tier key.

export CLOUDFLARE_API=

export GROQ_API_KEY=
export OPENROUTER_API_KEY=
export HF_API_KEY=
export DEEPINFRA_API_KEY=

export HETZNER_IP4_PUBLIC_IP=
export HETZNER_IP6_PUBLIC_IP=
export HETZNER_USER=
