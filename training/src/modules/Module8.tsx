import { ModuleStub } from './ModuleStub';
export default function Module8() {
  return <ModuleStub pillar={8} title="P-Gate Quorum" subtitle="Seven-Cycle Confirmation and the Sqrt(N)+2 Formula" description="No single node confirms a claim. This module trains the mechanics of the P-Gate: 7 confirmation cycles, quorum = min(N, ceil(sqrt(N)) + 2), and why that formula specifically resists capture by a coordinated minority while remaining reachable by an honest majority." />;
}
